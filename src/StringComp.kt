fun main() {
    var str1: String = "Aakash"

    var str2: String = "Aakash"

    // if (str1.equals(str2)) //you can pass 2nd argument as true to ignore case
    if (str1 == str2) // Compare strings in Kotlin
        println("Same")
    else
        println("Not Same")
}